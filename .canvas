<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>cymatics</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body { 
  background: #0a0a0f; 
  overflow: hidden; 
  cursor: crosshair;
}
canvas { display: block; }
#hint {
  position: fixed;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  color: rgba(255, 50, 150, 0.4);
  font-family: 'Courier New', monospace;
  font-size: 12px;
  letter-spacing: 2px;
  pointer-events: none;
  opacity: 1;
  transition: opacity 2s ease;
}
</style>
</head>
<body>
<canvas id="canvas"></canvas>
<div id="hint">click to strike the membrane</div>
<script>
const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');
const hint = document.getElementById('hint');

let width, height, gridW, gridH;
const cellSize = 4;
let current, previous, damping = 0.985;

// Web Audio setup
let audioCtx = null;
const initAudio = () => {
  if (!audioCtx) {
    audioCtx = new (window.AudioContext || window.webkitAudioContext)();
  }
};

// Create resonant tone on strike
const playTone = (x, y, intensity) => {
  if (!audioCtx) return;
  
  const osc = audioCtx.createOscillator();
  const gain = audioCtx.createGain();
  const filter = audioCtx.createBiquadFilter();
  
  // Frequency based on position (creates harmonic relationship)
  const baseFreq = 80;
  const xHarmonic = Math.floor((x / gridW) * 8) + 1;
  const yHarmonic = Math.floor((y / gridH) * 6) + 1;
  const freq = baseFreq * (xHarmonic + yHarmonic * 0.5);
  
  osc.type = 'sine';
  osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
  osc.frequency.exponentialRampToValueAtTime(freq * 0.5, audioCtx.currentTime + 1.5);
  
  filter.type = 'lowpass';
  filter.frequency.setValueAtTime(2000, audioCtx.currentTime);
  filter.Q.setValueAtTime(8, audioCtx.currentTime);
  
  gain.gain.setValueAtTime(Math.min(0.15, intensity * 0.003), audioCtx.currentTime);
  gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 1.5);
  
  osc.connect(filter);
  filter.connect(gain);
  gain.connect(audioCtx.destination);
  
  osc.start();
  osc.stop(audioCtx.currentTime + 1.5);
};

function resize() {
  width = canvas.width = window.innerWidth;
  height = canvas.height = window.innerHeight;
  gridW = Math.ceil(width / cellSize);
  gridH = Math.ceil(height / cellSize);
  
  current = new Float32Array(gridW * gridH);
  previous = new Float32Array(gridW * gridH);
}

function idx(x, y) {
  return y * gridW + x;
}

// Strike the membrane at a point
function strike(cx, cy, radius, force) {
  const gx = Math.floor(cx / cellSize);
  const gy = Math.floor(cy / cellSize);
  
  for (let dy = -radius; dy <= radius; dy++) {
    for (let dx = -radius; dx <= radius; dx++) {
      const x = gx + dx;
      const y = gy + dy;
      if (x < 0 || x >= gridW || y < 0 || y >= gridH) continue;
      
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist < radius) {
        const falloff = 1 - (dist / radius);
        const wave = Math.cos(dist * 0.5) * falloff * falloff;
        current[idx(x, y)] += force * wave;
      }
    }
  }
  
  playTone(gx, gy, force);
}

// 2D wave equation simulation
function simulate() {
  const next = new Float32Array(gridW * gridH);
  
  for (let y = 1; y < gridH - 1; y++) {
    for (let x = 1; x < gridW - 1; x++) {
      const i = idx(x, y);
      
      // Laplacian (neighbors average)
      const neighbors = 
        current[idx(x-1, y)] + 
        current[idx(x+1, y)] + 
        current[idx(x, y-1)] + 
        current[idx(x, y+1)];
      
      // Wave equation: acceleration proportional to curvature
      next[i] = (neighbors / 2) - previous[i];
      next[i] *= damping;
    }
  }
  
  previous = current;
  current = next;
}

function render() {
  // Dark fade with subtle trail
  ctx.fillStyle = 'rgba(10, 10, 15, 0.15)';
  ctx.fillRect(0, 0, width, height);
  
  const imageData = ctx.createImageData(width, height);
  const data = imageData.data;
  
  for (let y = 0; y < gridH; y++) {
    for (let x = 0; x < gridW; x++) {
      const val = current[idx(x, y)];
      const absVal = Math.abs(val);
      
      if (absVal > 0.1) {
        // Map value to colors
        // Positive = cyan, negative = pink, intensity = brightness
        const intensity = Math.min(1, absVal / 30);
        
        let r, g, b;
        if (val > 0) {
          // Cyan to white
          r = Math.floor(50 + intensity * 200);
          g = Math.floor(200 + intensity * 55);
          b = Math.floor(220 + intensity * 35);
        } else {
          // Pink/magenta to white
          r = Math.floor(220 + intensity * 35);
          g = Math.floor(50 + intensity * 150);
          b = Math.floor(180 + intensity * 75);
        }
        
        // Draw the cell
        for (let py = 0; py < cellSize; py++) {
          for (let px = 0; px < cellSize; px++) {
            const screenX = x * cellSize + px;
            const screenY = y * cellSize + py;
            if (screenX >= width || screenY >= height) continue;
            
            const pi = (screenY * width + screenX) * 4;
            const edgeFade = (px === 0 || py === 0) ? 0.7 : 1;
            data[pi] = r * edgeFade;
            data[pi + 1] = g * edgeFade;
            data[pi + 2] = b * edgeFade;
            data[pi + 3] = Math.floor(intensity * 255 * edgeFade);
          }
        }
      }
    }
  }
  
  ctx.putImageData(imageData, 0, 0);
  
  // Add glow layer
  ctx.globalCompositeOperation = 'screen';
  ctx.filter = 'blur(8px)';
  ctx.globalAlpha = 0.3;
  ctx.drawImage(canvas, 0, 0);
  ctx.filter = 'none';
  ctx.globalAlpha = 1;
  ctx.globalCompositeOperation = 'source-over';
}

// Auto-strike for ambient animation
let autoStrikeTimer = 0;
function autoStrike() {
  autoStrikeTimer++;
  if (autoStrikeTimer > 120) { // Every ~2 seconds
    const x = Math.random() * width;
    const y = Math.random() * height;
    const radius = 8 + Math.random() * 12;
    const force = 20 + Math.random() * 30;
    strike(x, y, radius, force);
    autoStrikeTimer = Math.random() * 60; // Randomize next
  }
}

function animate() {
  simulate();
  autoStrike();
  render();
  requestAnimationFrame(animate);
}

// Mouse interaction
let isPressed = false;
let lastStrike = 0;

canvas.addEventListener('mousedown', (e) => {
  initAudio();
  isPressed = true;
  hint.style.opacity = '0';
  strike(e.clientX, e.clientY, 15 + Math.random() * 10, 50);
  lastStrike = Date.now();
});

canvas.addEventListener('mousemove', (e) => {
  if (isPressed && Date.now() - lastStrike > 50) {
    strike(e.clientX, e.clientY, 8, 25);
    lastStrike = Date.now();
  }
});

canvas.addEventListener('mouseup', () => isPressed = false);
canvas.addEventListener('mouseleave', () => isPressed = false);

// Touch support
canvas.addEventListener('touchstart', (e) => {
  e.preventDefault();
  initAudio();
  hint.style.opacity = '0';
  const touch = e.touches[0];
  strike(touch.clientX, touch.clientY, 15, 50);
});

canvas.addEventListener('touchmove', (e) => {
  e.preventDefault();
  const touch = e.touches[0];
  strike(touch.clientX, touch.clientY, 8, 25);
});

window.addEventListener('resize', resize);
resize();

// Initial ambient strikes
setTimeout(() => strike(width * 0.3, height * 0.4, 12, 35), 500);
setTimeout(() => strike(width * 0.7, height * 0.6, 10, 30), 1200);
setTimeout(() => strike(width * 0.5, height * 0.3, 14, 40), 2000);

animate();
</script>
</body>
</html>
